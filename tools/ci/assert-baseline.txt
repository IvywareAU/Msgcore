# ASSERT-family baseline. Generated by tools/ci/check_asserts.ps1 -Regenerate.
#
# These are ceilings, not targets. CI fails when a count RISES; a fall is a
# notice and should be banked here in the same commit that earned it, or the
# stale ceiling silently re-admits everything between the two numbers.
#
# See the release-readiness register, item 19 for what each form costs in Release.

callwrap 259
# marker rose 257 -> 258 in 7e5b3ba: P3PmsgData::c_vBlobCopy guards a blob-type
# mismatch with ASSERT(0) immediately before an unconditional EVERR->Throw().
# Banked rather than deleted -- it is a debug-break-at-origin aid matching 13
# sibling EVERR sites in P2Pmsg.cpp, not structural validation, and the throw
# beside it runs in every build, so Release loses nothing.
marker 258
predicate 215
